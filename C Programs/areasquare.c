#include <stdio.h>
#include <conio.h>
void main ()
{
    int height, width, area;
    height = 5, width = 4;
    area = height*width;
    printf("Height = %d m \n", height);
    printf("Width = %d m \n", width);
    printf("Area = %d sq. metre", area);
    getch();
}
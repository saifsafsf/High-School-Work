#include <stdio.h>
#include <conio.h>
void main ()
{
    char a[] = "guardians of the galaxy vol. 2";
    printf("the size of long is %d\n", sizeof(long));
    printf("the size of int is %d \n", sizeof(int));
    printf("the size of a is %d\n", sizeof(a));
    getch();
}